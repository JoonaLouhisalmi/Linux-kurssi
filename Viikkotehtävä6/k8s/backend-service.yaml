apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8000        # Service port
      targetPort: 8000  # Container port
      nodePort: 30080   # Fixed NodePort (choose 30000–32767)
